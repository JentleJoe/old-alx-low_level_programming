#include "main.h"
#include <string.h>

/**
 * _strcat:
 *
 * Return: concartenation result
 */

char *_strcat(char *dest, char *src)
{

/*	dest[(strlen(dest) - 1)];
	src[(strlen(src) - 1)];

	return (_strcat(dest, src));
*/
}


